
{% extends "GestorDeProjectesBundle:Default:index.html.twig " %} 
{% block body %}
	{{form_start(form)}}
		{{form_row(form.username)}}
		{{form_row(form.role)}}
		{{form_row(form.email)}}
		{{form_row(form.plainPassword.first)}}
		{{form_row(form.plainPassword.second)}}
		{{form_row(form.guardar)}}
	{{form_end(form)}}
{% endblock %}
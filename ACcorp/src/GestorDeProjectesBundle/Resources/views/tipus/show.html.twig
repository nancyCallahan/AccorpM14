{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipus</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipus.id }}</td>
            </tr>
            <tr>
                <th>Descripcio</th>
                <td>{{ tipus.descripcio }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipus_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipus_edit', { 'id': tipus.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

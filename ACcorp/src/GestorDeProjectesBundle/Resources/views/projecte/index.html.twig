{% extends 'base.html.twig' %}

{% block body %}
    <h1>Projectes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Descripciocurta</th>
                <th>Descripciollarga</th>
                <th>Entorn</th>
                <th>Imatge</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projecte in projectes %}
            <div>
                <div><img  src="{{asset("../web/img/"~projecte.imatge)}}"></div>
                <div>{{ projecte.nom }}</div>
                <div>{{ projecte.descripcioCurta }}</div>
                <div>{{ projecte.entorn }}</div>
                <div>{{ projecte.descripcioLlarga }}</div>
            </div>

           

                    <ul>
                        <li>
                            <a href="{{ path('projecte_show', { 'id': projecte.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('projecte_edit', { 'id': projecte.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('projecte_new') }}">Create a new projecte</a>
        </li>
    </ul>
{% endblock %}


